function threeSum(arr, target) {
// write your code here
	 let target = '';
  while (arr > 0 || target.length < bits) {
    target = (arr % 2) + target;
    arr = Math.floor(arr / 2);
  }
  return target;
  
}

module.exports = threeSum;
